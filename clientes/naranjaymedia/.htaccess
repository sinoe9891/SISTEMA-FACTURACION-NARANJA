# ==========================================================
#  NARANJA - clientes/naranjaymedia/.htaccess
#  Funciona en local (XAMPP) y en producción (LiteSpeed/cPanel)
#  SIN redirects externos para evitar loops en LiteSpeed
# ==========================================================

RewriteEngine On

# Pasar directo sin modificar
RewriteRule ^includes/ - [L]
RewriteRule ^acciones/ - [L]

# Rewrite interno: /pagina → /pagina.php
# Solo si el archivo .php existe y no es directorio ni archivo real
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}\.php -f
RewriteRule ^(.+)$ $1.php [L]

Options -Indexes
